# MNIST Example

The following is an example of the ttl library to implement a neural network to train on the mnist dataset.
If the dataset doesnt exist it can be fetched, see --help.

Dependencies:
    ttl
    curl